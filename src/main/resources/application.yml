spring:
  jpa:
    show-sql: true
    generate-ddl: false
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect


  datasource:
    url: jdbc:postgresql://localhost:5432/pstuedu
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  flyway:
    locations: classpath:db/migration

#keycloak:
#  auth-server-url: http://localhost:9080/auth
#  realm: pstuEdu
#  resource: pstuEdu_client
#  public-client: true
