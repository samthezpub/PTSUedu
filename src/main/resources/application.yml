spring:
  jpa:
    show-sql: true
    generate-ddl: false
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect


  datasource:
    url: jdbc:postgresql://localhost:5432/ptsuedu
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver

#keycloak:
#  auth-server-url: http://localhost:9080/auth
#  realm: pstuEdu
#  resource: pstuEdu_client
#  public-client: true
